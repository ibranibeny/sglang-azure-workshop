#cloud-config
# Installs Docker + NVIDIA Container Toolkit so the SGLang container can use GPUs.
# The NVIDIA host driver itself is installed by the NvidiaGpuDriverLinux VM
# extension applied in 01-deploy.sh.
package_update: true
package_upgrade: false
packages:
  - ca-certificates
  - curl
  - gnupg

write_files:
  - path: /opt/sglang/.keep
    content: ""

runcmd:
  # --- Docker ----------------------------------------------------------------
  - curl -fsSL https://get.docker.com | sh
  - systemctl enable --now docker
  # --- NVIDIA Container Toolkit ----------------------------------------------
  - install -m 0755 -d /usr/share/keyrings
  - curl -fsSL https://nvidia.github.io/libnvidia-container/gpgkey | gpg --dearmor -o /usr/share/keyrings/nvidia-container-toolkit-keyring.gpg
  - curl -s -L https://nvidia.github.io/libnvidia-container/stable/deb/nvidia-container-toolkit.list | sed 's#deb https://#deb [signed-by=/usr/share/keyrings/nvidia-container-toolkit-keyring.gpg] https://#g' | tee /etc/apt/sources.list.d/nvidia-container-toolkit.list
  - apt-get update
  - apt-get install -y nvidia-container-toolkit
  - nvidia-ctk runtime configure --runtime=docker
  - systemctl restart docker
  - mkdir -p /opt/hf-cache
